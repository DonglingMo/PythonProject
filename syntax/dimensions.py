dimensions = (200, 50)
print(dimensions[0])
print(dimensions[1])

for dimension in dimensions:
    print(dimension)

# 重新赋值
dimensions = (400, 100)
for dimension in dimensions:
    print(dimension)
# 存储不可变数据，只读
# 字典
# 多返回值
# 解包元组，变量分配
# 函数参数，固定的顺序和数量
# 常量
